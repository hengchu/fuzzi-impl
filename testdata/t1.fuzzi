tx :        int;
ty :        int;
i  :        int;
xs :[1.0] { int };
ys :      { int };

r1 : { { foo : int, bar : int } };
r2 : { foo : int, bar : int };

bmap(xs, ys, tx, i, ty, {
  ty = tx * 2;
});

r1[0] = r2;