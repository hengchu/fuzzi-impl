tx :        int;
ty :        int;
i  :        int;
xs :[1.0] { int };
ys :      { int };

xs1 :[1.0] [ int ];
ys1 :      [ int ];

bmap(xs, ys, tx, i, ty, {
  ty = tx * 2;
});

amap(xs1, ys1, tx, i, ty, {
  ty = tx * 2;
});

zs :[1.0] { int };
zs2 : { { int } };

j : int;

partition(zs, zs2, tx, i, j, 10, {
  j = tx * 2;
});
